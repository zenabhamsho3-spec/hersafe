<?xml version="1.0" encoding="utf-8"?>
<selector xmlns:android="http://schemas.android.com/apk/res/android">
    <!-- الحالة عندما يكون العنصر مختاراً (يصبح لونه رمادي) -->
    <item android:state_checked="true" android:drawable="@color/item_selected_gray" />

    <!-- الحالة العادية (شفاف) -->
    <item android:drawable="@android:color/transparent" />
</selector>